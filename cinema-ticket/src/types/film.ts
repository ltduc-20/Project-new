export type film = {
  film_name: string;
  director?: string;
  thumbnail_url?: string;
  trailer_url?: string;
  description?: string;
  genre_id?: number;
  rating_id?: number;
  release_date?: string;
  is_showing?: boolean;
};